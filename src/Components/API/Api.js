const baseUrl = 'https://v1.bellapp.in/';

export const servicesUrl = `${baseUrl}services`;

export const companiesUrl = `${baseUrl}service-companies/`

export const reviewsUrl = `${baseUrl}reviews/`

export const detailUrl = `${baseUrl}company-detail/`

export const queryUrl = `${baseUrl}enquiry`

export const appointmentUrl = `${baseUrl}appointment`

export const bannerUrl = `${baseUrl}banner/`

export const searchUrl = `${baseUrl}search`

export const searchResultsUrl = `${baseUrl}search-results`

export const randomCompaniesUrl = `${baseUrl}random-companies/`

export const advertisingUrl  = `${baseUrl}advertisements`